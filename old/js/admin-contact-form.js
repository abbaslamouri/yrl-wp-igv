
jQuery(document).ready(function($) {

	"use strict";

	

	// // set field id based on field label for contact form fields
	// var fieldLabel = $(".axl_wp_ultimate_contact_form input#field_label");
	// $(".axl_wp_ultimate_contact_form").on('change', fieldLabel, function(event) {
	// 	var fieldLabelVal = $(this).find("input#field_label").val();
	// 	$(".axl_wp_ultimate_contact_form input#field_id").val(fieldLabelVal.split(' ').join('-').toLowerCase());
	// });


	// // Show or hide field options depending on field type
	// var fieldType = $('#axl_wp_ultimate_contact_form select#field_type');  
	// var fieldOptions = $('#axl_wp_ultimate_contact_form tr#field_options');
	// if (fieldType.val() == 'select'  || fieldType.val() == 'radio') {
	// 	fieldOptions.show();
	// } else {
	// 	fieldOptions.hide();
	// }
	// $(fieldType).change(function(event) { 
	// 	if ( $(this).val() == 'select' || $(this).val() == 'radio' ) {
	// 		$(fieldOptions).show();
	// 		$(fieldOptions).find('input').attr('required', true);
	// 	} else {
	// 		$(fieldOptions).hide();
	// 		$(fieldOptions).find('input').attr('required', false);
	// 	}
	// });



	

});  // END jQuery(document).ready(function($) {
